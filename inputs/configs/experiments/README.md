# experiments/ — X×Y×Z 实验矩阵定义

每个 `.toml` 文件定义一次全量扫荡或迭代训练实验的矩阵参数。

格式见 `docs/architecture/xyz_test_matrix.md`。

当前状态（MS-R3-001）：旧 rolling baseline/fastpilot TOML 已删除（引用不存在的 `data/datasets/...`）。待 MS-R4 数据湖落成后再补新实验矩阵。
