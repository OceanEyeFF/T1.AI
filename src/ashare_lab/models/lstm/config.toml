# LSTM 模型默认配置

[model]
name = "lstm"
input_dim = 19
hidden_size = 128
num_layers = 2
dropout = 0.2
loss_type = "l1"
loss_alpha = 0.3
ic_rank_beta = 0.5
norm_type = "layernorm"
norm_eps = 1e-5
max_epochs = 50
patience = 10
