# XGBoost 模型默认配置

[model]
name = "xgboost"
n_estimators = 200
max_depth = 6
learning_rate = 0.1
subsample = 0.8
colsample_bytree = 0.8
min_child_weight = 1.0
gamma = 0.0
reg_alpha = 0.0
reg_lambda = 1.0
n_jobs = -1
early_stopping_rounds = 20
device = "cpu"
random_seed = 42
