# Transformer 模型默认配置

[model]
name = "transformer"
input_dim = 6
d_model = 128
n_heads = 4
n_layers = 4
d_ff = 512
dropout = 0.1
max_seq_len = 512
min_seq_len = 20
loss_weights = [1.0, 1.0, 1.0]
loss_type = "l1"
loss_alpha = 0.3
max_epochs = 50
