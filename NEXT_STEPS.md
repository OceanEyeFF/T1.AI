# 下一步工作安排（Roadmap）

当前仓库目标：在严格模拟 A 股约束（T+1、涨跌停/一字板、成交失败、总摩擦成本 `max(5元, 成交额*0.001)`、单日亏损阈值 2%）下，做“**日频评估、低换手执行**”的选股与仓位管理研究；模型（LSTM/Transformer）作为第二层增值组件，优先保证回测与风控口径正确。

相关基线文档：
- 约束：`docs/constraints.md`
- 目标/验收口径：`docs/objectives.md`
- 数据契约：`docs/data_contract.md`
- 数据来源/新闻建议：`docs/data_sources.md`、`docs/news_sources.md`

---

## 0. 明确交易协议（先定口径，避免模型返工）✅

**状态：已完成（2025-12-29）**

目标：把"什么时候产生信号、什么时候成交、持有周期、调仓频率、做T范围"写成可配置协议（V1 先用日线，V2 再扩分钟线做T）。

已完成：
- [x] 信号时点：默认收盘后计算（t 日 close 可用）
- [x] 成交时点：默认次日开盘成交（t+1 open）
- [x] 持有周期：默认持有到下一次调仓
- [x] 做T（V1 日线版）：先不做真实日内T；只保留"先卖后买回"的撮合能力
- [x] 做T（V2 分钟线版）：引入分钟线后再实现"盘中卖出→盘中/尾盘买回"的策略规划

交付物：
- ✅ `docs/protocol.md`（交易协议与可用信息集合）
- ✅ `configs/protocol.yaml`（可配置）
- ✅ README.md 已更新（添加协议文档引用）

---

## 1. 股票池与可交易性（让"每天评估"变得可控）✅

**状态：已完成（2025-12-29）**

目标：从 akshare 拉取全市场列表，按硬约束过滤，并给出可复现的"每日股票池快照"。

已完成：
- [x] 新增 `scripts/build_universe.py`：拉取 A 股列表并过滤（排除 ST/北交/科创/创业）
- [x] 增强过滤函数：添加 6 位数字长度验证
- [x] 保存快照：`data/cache/universe/<date>.csv`
- [x] 单元测试：`tests/test_universe.py`（验证过滤规则正确性）

交付物：
- ✅ `scripts/build_universe.py`（股票池构建脚本）
- ✅ `src/ashare_lab/universe.py`（增强的过滤函数）
- ✅ `tests/test_universe.py`（单元测试，12 个测试全部通过）
- ✅ README.md 已更新（添加使用说明）

---

## 2. 特征与标签（先做简单、严格防穿越）

目标：搭建 dataset builder（特征/标签/对齐规则），为规则策略与深度模型共用。

计划：
- [ ] `src/ashare_lab/features/`：基础特征（收益率、均线、波动率、量价等），全部滞后对齐
- [ ] `src/ashare_lab/labels/`：明确标签定义（如：次日收益、次日相对沪深300超额）
- [ ] `scripts/build_dataset.py`：从 universe + bars 生成可训练数据（支持 walk-forward 切分）

备注：
- V1 不接基本面；V1.1 再加“按披露日对齐”的财务特征。

---

## 3. 策略层（低换手执行：阈值、门槛、风控联动）

目标：把“每天都评估”变成“只有优势足够大才交易”，并且与单日亏损阈值联动。

计划：
- [ ] `src/ashare_lab/strategy/`：拆分为 `signal`（打分/排序）与 `portfolio`（目标权重/仓位）
- [ ] 引入换仓门槛：新候选相对当前持仓“优势>阈值”才换（降低成本侵蚀）
- [ ] 加入“成本覆盖阈值”：预期边际收益必须覆盖 `N` 倍成本才允许交易
- [ ] 风控行为明确化：触发 -2% 当日仅允许降风险、停止开新仓，并记录“因跌停/停牌无法执行”

---

## 4. 回测与报告（用一致口径评估：收益、风险、成本、可执行）

目标：让每次实验都自动产出对照一致的报告，避免“回测口径漂移”。

计划：
- [ ] 报告增强：月度超额、滚动 12 个月胜率、成本占毛利润、成交失败原因统计
- [ ] 输出统一：`runs/<timestamp>/` 中产出 `equity/benchmark/excess/fills/stats/diagnostics`

---

## 5. 模型架构（LSTM/Transformer 放在“增值层”，先定义接口）

目标：先定义“模型插槽”，让策略层只依赖统一的 `score` 输出；随后逐步替换为深度模型。

计划：
- [ ] `src/ashare_lab/models/base.py`：统一接口（fit/predict/feature_schema/version）
- [ ] Baseline：先做一个轻量模型（可选：线性/树模型）用于验证 pipeline 与 walk-forward
- [ ] LSTM：序列特征（过去 N 日）→ 预测下一期超额/方向
- [ ] Transformer：同样走序列输入，重点做正则/早停/滚动训练，避免过拟合

关键点（必须在实现时写进文档/代码）：
- 训练/验证切分：walk-forward
- 目标：以“相对沪深300超额”为主（与你的目标口径一致）
- 模型输出只做排序/打分，不直接决定下单（下单由策略层门槛与风控控制）

---

## 6. 新闻/公告插件（后置，但先把接口留好）

目标：先把“文本事件 schema + 存档 + 抽取 JSON”做成可插拔，不影响主链路。

计划：
- [ ] `src/ashare_lab/text_events/`：schema 与存档
- [ ] akshare 作为备选源；默认不启用，避免时间戳/穿越/ToS 风险
- [ ] 抽取器：LLM 输出结构化事件（利好/利空/类型/强度），原文必须存档

---

## 7. 下一步工作规划（2025-12-29 更新）

### ✅ 已完成任务

- **任务 0：交易协议定义**（2025-12-29 完成）
  - 创建 `docs/protocol.md` 和 `configs/protocol.yaml`
  - 明确信号/成交时点、持有周期、做T策略等核心协议

- **任务 1：股票池与可交易性**（2025-12-29 完成）
  - 创建 `scripts/build_universe.py` 股票池构建脚本
  - 增强 `src/ashare_lab/universe.py` 过滤函数（6位数字验证）
  - 添加单元测试 `tests/test_universe.py`（12 个测试全部通过）
  - 更新 README.md 添加使用说明

---

### 优先级 1：特征与标签（任务 2）⭐ **下一步**

**目标：** 搭建 dataset builder（特征/标签/对齐规则），为规则策略与深度模型共用。

**计划步骤：**
1. `src/ashare_lab/features/`：基础特征（收益率、均线、波动率、量价等），全部滞后对齐
2. `src/ashare_lab/labels/`：明确标签定义（如：次日收益、次日相对沪深300超额）
3. `scripts/build_dataset.py`：从 universe + bars 生成可训练数据（支持 walk-forward 切分）
4. 单元测试：验证特征/标签时间对齐，防穿越

**验收标准：**
- ✅ 特征计算严格滞后对齐（t 日特征仅使用 t-1 及之前数据）
- ✅ 标签定义清晰（次日相对基准超额收益）
- ✅ dataset builder 支持 walk-forward 切分（训练集/验证集/测试集）
- ✅ 通过 gate 验证

**预计工作量：** 1 天

**前置依赖：** 任务 1 已完成（股票池快照机制）

---

### 优先级 2：策略层增强（任务 3）

**目标：** 把"每天都评估"变成"只有优势足够大才交易"，并且与单日亏损阈值联动。

**计划步骤：**
1. `src/ashare_lab/strategy/`：拆分为 `signal`（打分/排序）与 `portfolio`（目标权重/仓位）
2. 引入换仓门槛：新候选相对当前持仓"优势>阈值"才换（降低成本侵蚀）
3. 加入"成本覆盖阈值"：预期边际收益必须覆盖 N 倍成本才允许交易
4. 风控行为明确化：触发 -2% 当日仅允许降风险、停止开新仓，并记录"因跌停/停牌无法执行"
5. 集成到回测引擎：`BacktestEngine` 调用策略层新接口

**验收标准：**
- ✅ 策略层拆分为 signal + portfolio 两层
- ✅ 换仓门槛与成本覆盖阈值可配置（`configs/protocol.yaml`）
- ✅ 风控行为明确化（回测报告显示成交失败原因统计）
- ✅ 通过 gate 验证

**预计工作量：** 1.5 天

**前置依赖：** 任务 2 已完成（特征/标签 pipeline）

---

### 里程碑目标

**V0.5 版本（当前进度：2/7）**

已完成：
- ✅ **任务 0**：完整的交易协议定义（信号/成交时点、持有周期、做T策略）
- ✅ **任务 1**：可复现的股票池快照机制

进行中：
- ⭐ **任务 2**：特征/标签 pipeline（为深度模型铺路）← **下一步**

计划中：
- 🔲 **任务 3**：低换手策略层（换仓门槛 + 成本覆盖阈值）
- 🔲 **任务 4**：回测与报告增强（月度超额、滚动胜率、成本占比统计）
- 🔲 **任务 5**：模型架构接口定义（LSTM/Transformer 插槽）
- 🔲 **任务 6**：新闻/公告插件（文本事件 schema + 存档 + 抽取）

**V1.0 版本规划：**
- 基本面数据接入（按 `announce_date` 对齐）
- LSTM/Transformer 模型实现与回测验证
- 完整的 walk-forward 验证流程
- 生产环境部署准备（监控、告警、日志）

---

### 下一步行动建议

主人可以继续使用 `/autoworkflow:feature-shipper` 命令，浮浮酱会自动执行任务 2（特征与标签）喵～ (´。• ᵕ •。`) ♡

或者主人有其他优先级更高的任务也可以告诉浮浮酱喵～ φ(≧ω≦*)♪

