---
title: "S1-A3-T2 XGBoost Report Contract"
artifact_type: "worktrack-task-evidence"
updated: "2026-06-16T15:35:00+08:00"
owner: "OceanEyeFF"
---

# S1-A3-T2 XGBoost Report Contract

## Control Signal

- task_id: S1-A3-T2
- task_title: Define minimal XGBoost report contract patch
- task_status: completed
- scope_result: bounded contract definition completed
- recommended_next_action: S1-A3-T3 Implement or wire report contract compliance
- implementation_target: `scripts/run_xgboost_rolling_retrain_regime.py`
- checker_target: no checker relaxation
- blocked_by_data: no for future XGBoost writer output; historical reports without OOS parquet remain data-limited for strict daily-CS.

## Required Report Fields

- top-level `evaluation_protocol`
  - `signal_time_mode`: `close`
  - `execution_time_mode`: `next_open`
  - `label_mode`: dataset label mode loaded by `_load_label_mode`
  - `return_mode`: same value as `label_mode`
  - `cost_model`: `none`
  - `daily_cs_mode`: `required`
- top-level `comparison_panel`
  - built from the already materialized OOS prediction frame.
  - uses the existing canonical `build_primary_trade_like_comparison_panel` helper.
  - default `top_n` should match LSTM: `10`.
- top-level `oos_predictions_path`
  - remains explicit only when `--save-oos-parquet` is passed.
  - no implicit path should be invented for historical reports or runs that did not write parquet.

## Compatibility Rules

- Preserve existing metric blocks and config fields.
- Preserve report compatibility for consumers that read `raw_oos_metrics`, `calibrated_oos_metrics`, `weekly_logs`, or `monthly_logs`.
- Do not weaken `compare_ic_reports.py --check-protocol`; missing protocol should continue to fail.
- Do not treat `comparison_panel` as a substitute for daily-CS OOS parquet evidence.
- Do not backfill historical report files in-place.

## Implementation Plan

- Import `build_primary_trade_like_comparison_panel` in `scripts/run_xgboost_rolling_retrain_regime.py`.
- Add a local `_build_evaluation_protocol(label_mode: str) -> dict[str, str]` helper.
- Add a local `_build_comparison_panel(oos: pd.DataFrame, *, top_n: int) -> dict[str, object]` helper mirroring the LSTM wrapper.
- Add parser argument `--comparison-top-n` with default `10`.
- Add `comparison_top_n` to the report config block.
- Add `evaluation_protocol` and `comparison_panel` to the final report dict.

## Validation Plan

- Add focused unit tests that import the new helpers and run on a small in-memory OOS DataFrame.
- Run focused tests only; no XGBoost training or provider calls.
- Run a smoke protocol check against tiny fixture reports generated by the test or existing checker tests.

## Scope Control

- provider_calls: none
- long_training: none
- dependency_changes: none
- production_artifacts: none
- checker_relaxation: none
- commit_push_release: none
