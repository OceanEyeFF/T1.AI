# 项目长远规划（ROADMAP）

**文档颗粒度：** `overview`  
**时间属性：** `future` / `long_term`  
**作用：** 长期路线入口，回答“项目未来要变成什么样，以及按什么顺序演进”。

## 战略定位

T1.AI 的长期目标，不是单纯堆出一个更复杂的模型，而是建设一套：

- **研究口径可信**：避免泄漏、成本低估与回测幻觉；
- **执行口径可审计**：从分数到订单到成交都有解释链；
- **模型演进可分线管理**：共享基础设施，隔离主线与实验线；
- **后续可产品化**：为调度、监控、接口与可视化保留空间。

## 当前路线冻结（2026-06）

当前路线明确冻结为三条开发线：

1. **`3d/5d/10d` 短期预测模型线**
   - 当前默认主 alpha 线；
   - 当前仍需证明预测可用性；
   - 负责稳定可信评估范式、优化闭环和 `pred_3d/pred_5d/pred_10d -> alpha_score`。

2. **`1d` 超快速预测线**
   - 独立研究线；
   - 必须先验证日内 / 分钟级数据可用性；
   - 不直接进入当前主线交易打分。

3. **决策模型线**
   - 负责把预测分数、持仓、成本、风险状态和 A 股交易约束转为可审计交易动作；
   - 可先冻结 I/O 协议，但复杂决策实现应等待主线 `alpha_score` 通过可信评估门禁。

长期路线都应服从这三个前提，不能反向把主线重新搅乱。

## 阶段 S0：执行层与主线边界收敛（当前阶段）

**时间范围：** 2026Q1 ~ 2026Q2
**目标：** 把“研究能跑”升级为“交易链可解释、模型线不混线”。

### 关键交付物

- 换仓门槛、成本覆盖、风险禁买、成交阻断统计进入默认执行链；
- `ashare_exec`（Decision → WeightMapper）与 `BacktestEngine` 的诊断字段闭环；
- 主线 `3d/5d/10d` 可信评估范式固定；
- 主线伪信号 / 泄漏 / 过拟合排查进入默认门禁；
- 主线 `3d/5d/10d` 默认配置、默认门禁、默认报告冻结；
- 主线 `pred_3d/pred_5d/pred_10d -> alpha_score` 聚合契约固定，并进入默认推荐输出；
- `1d` 独立研究入口、独立配置与独立报告边界固定；
- `1d` 分钟级数据可用性验证完成，避免用日 K-only 结果给超快速预测下最终结论；
- 决策模型输入 / 输出协议冻结。

### Gate

- 默认回测结果能解释“为什么交易 / 为什么不交易”；
- 主线和 `1d` 的代码、配置、报告边界清晰；
- 主线预测能通过可信评估门禁，或明确给出 no-go / continue-research 结论；
- 决策模型能在固定主线候选信号输入下复跑，但不把未验证信号当成默认可交易信号；
- 新增模型不再靠混用脚本与临时参数维持。

## 阶段 S1：主线模型稳态迭代（2026Q2 ~ 2026Q3）

**目标：** 在稳定执行层之上，持续优化 `3d/5d/10d` 主线信号质量。

### 关键交付物

- 主线统一比较面板（IC / RankIC / 月胜率 / 最差月 / 连续负月 / 执行后换手与成本）；
- 主线模型家族比较报告（如 XGB、LSTM、轻量融合）；
- 主线实验配置、数据切分、产物输出完全配置化；
- 主线聚合分数、逐头诊断字段与推荐导出协议保持一致；
- 统一的主线门禁，保证实验结论可横向比较。

### Gate

- 任一主线增量都必须在统一 OOS 与统一执行层下验证；
- 不允许以“局部时间窗更好”替代稳定性证明；
- 若收益提升依赖换手放大且成本不可控，则不进入默认主线。

## 阶段 S1b：`1d` 超快速预测数据与模型验证（条件阶段）

**目标：** 只在分钟级数据验证通过后，判断 `1d` 是否值得作为独立超短线预测系统继续投入。

### 关键交付物

- TuShare `stk_mins` 或其他分钟级数据源的覆盖率、历史长度、权限、字段和成本报告；
- AkShare 分钟接口的 smoke test 结果；
- `1d` 标签和日内特征协议；
- XGBoost / LSTM 的 `1d direction` 独立报告；
- go / no-go 判断。

### Gate

- 未通过分钟级数据验证时，不进入复杂 `1d` 模型；
- `1d` 结果不影响主线 `alpha_score`；
- 任一 `1d` 结论必须明确成本、换手和噪声约束。

## 阶段 S2：研究到执行的产品化底座（2026H2）

**目标：** 把研究链和执行链变成可持续运行的工程系统。

### 关键交付物

- 标准化训练与回测元数据写出；
- 结构化日志、每日调度、失败重试与监控告警；
- 历史信号、交易记录、回测报告的统一存储；
- 面向内部使用的查询接口或轻量服务层。

### Gate

- 单日运行结果可追溯到数据版本、配置版本与模型版本；
- 默认链路支持定时运行与失败诊断；
- 没有人工翻日志也能定位主要异常类型。

## 阶段 S3：后置插件与扩展能力（2027+）

**目标：** 在主链稳定后，再扩展更复杂的数据与策略能力。

### 候选方向

- 财报与估值特征的稳定化接入；
- 新闻 / 公告 / 事件类插件；
- Regime / 漂移 / 风险状态机；
- 面向更完整研究协作的可视化看板。

### 进入条件

- 执行层已经稳定，不再是主风险；
- 主线 `3d/5d/10d` 已形成可持续的比较与上线门禁；
- 新插件不会反向污染默认主线口径。

## 长期约束

- 不在主线闭环之前用更复杂模型掩盖执行层问题；
- 不在 `1d` 独立门禁通过前把 `1d` 并入主线；
- 任何新能力都应先判断是“共用基础设施”还是“模型线专用逻辑”；
- 入口文档必须和代码现实同步，避免路线图再次落后于实际开发。

## 关联文档

- 当前执行入口：[NEXT_STEPS.md](NEXT_STEPS.md)
- 文档总导航：[docs/README.md](docs/README.md)
- 全局工作规则：[docs/WORK_RULES.md](docs/WORK_RULES.md)
- 主线评估门禁：[docs/research/mainline_3510d_evaluation_gate_protocol.md](docs/research/mainline_3510d_evaluation_gate_protocol.md)
- 研究清单：[docs/research/research_checklist.md](docs/research/research_checklist.md)
- 仓库结构：[docs/architecture/repo_structure_guide.md](docs/architecture/repo_structure_guide.md)
- 核心研究方法论：[docs/research/](docs/research/)
- 已归档旧文档：[docs/archive/](docs/archive/)
